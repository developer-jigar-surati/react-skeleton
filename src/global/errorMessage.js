
export const requiredFiledMessage = (field) => {
    return `${field} is required!`;
}

export const maxLengthMessage = (val) => {
    return `Maximum ${val} character allow.`;
}

export const minLengthMessage = (val) => {
    return `Minimum ${val} character allow.`;
}
